spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/note
    username: root
    password: yxc.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
  context-path: /note